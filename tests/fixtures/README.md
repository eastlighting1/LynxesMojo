# Shared Fixtures

This directory is reserved for cross-surface fixtures used by docs, examples,
Rust tests, CLI tests, and Python tests.

Phase 1 adds the fixture layout and keeps the canonical sample graph in
`examples/data/` as the user-facing source of truth.
